[core]
    remote = myremote
['remote "myremote"']
    url = s3://sbt-sast
    endpointurl = https://api.immers.cloud:8080
    access_key_id = ${DVC_ACCESS_KEY_ID}  # Используем переменную окружения
    secret_access_key = ${DVC_SECRET_ACCESS_KEY}
